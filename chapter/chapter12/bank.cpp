//
// Created by zkc on 2024/4/22.
//
#include <iostream>
#include "queue.h"

using namespace  std;
/*
int main(){
    int qsize ;
    Item temp;
    int i  = 0;
    int customers = 0;

    cout << "Enter max size of queue : " << endl;

    cin >> qsize;
    Queue line(qsize);

    while(!line.isfull()){

        temp.set(i++);
        line.enqueue(temp);
        customers++ ;
    }

    cout << "Customers " << customers <<endl;

    //出队列
    while(!line.isempty()){
        line.dequeue(temp);
        customers--;
    }
    cout << "now customers is " << customers << endl;
    return 0;
}
 */